namespace Abp.Dependency
{
    /// <summary>
    /// All classes implement this interface are automatically registered to dependency injection as transient object.
    /// 瞬态依赖，
    /// 所有实现这个接口的类，自动注册到依赖注入，作为临时对象。
    /// </summary>
    public interface ITransientDependency
    {

    }
}